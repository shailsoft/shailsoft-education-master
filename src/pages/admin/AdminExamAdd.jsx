import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminExamAdd() {
  return (
    <AdminPagePlaceholder
      title="Admin — Add Exam"
      description="Add a new exam to the schedule."
    />
  );
}
