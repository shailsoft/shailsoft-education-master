import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminImportData() {
  return (
    <AdminPagePlaceholder
      title="Admin — Import Data"
      description="Import data into the system from CSV/XLSX files."
    />
  );
}
