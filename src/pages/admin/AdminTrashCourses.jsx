import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminTrashCourses() {
  return (
    <AdminPagePlaceholder
      title="Admin — Trashed Courses"
      description="Courses moved to trash, with restore/delete actions."
    />
  );
}
