import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminUserAll() {
  return (
    <AdminPagePlaceholder
      title="Admin — All Users"
      description="All registered users with role and status filters."
    />
  );
}
