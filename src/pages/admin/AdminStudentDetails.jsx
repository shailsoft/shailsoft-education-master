import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminStudentDetails() {
  return (
    <AdminPagePlaceholder
      title="Admin — Student Details"
      description="View and edit a student record."
    />
  );
}
