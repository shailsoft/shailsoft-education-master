import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminSeminarAdd() {
  return (
    <AdminPagePlaceholder
      title="Admin — Add Seminar"
      description="Schedule a new seminar."
    />
  );
}
