import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminPageAll() {
  return (
    <AdminPagePlaceholder
      title="Admin — All Pages"
      description="All content pages managed through the admin panel."
    />
  );
}
