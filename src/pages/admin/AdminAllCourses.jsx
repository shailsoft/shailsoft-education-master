import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminAllCourses() {
  return (
    <AdminPagePlaceholder
      title="Admin — All Courses"
      description="List, edit, and manage every course offered."
    />
  );
}
