import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminAllMenu() {
  return (
    <AdminPagePlaceholder
      title="Admin — All Menus"
      description="Manage all navigation menus across the site."
    />
  );
}
