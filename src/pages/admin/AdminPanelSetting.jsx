import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminPanelSetting() {
  return (
    <AdminPagePlaceholder
      title="Admin — Panel Settings"
      description="Admin account and panel preferences."
    />
  );
}
