import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminSeminarEdit() {
  return (
    <AdminPagePlaceholder
      title="Admin — Edit Seminar"
      description="Update seminar details."
    />
  );
}
