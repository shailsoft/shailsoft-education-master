import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminAdmissionMenu() {
  return (
    <AdminPagePlaceholder
      title="Admin — Admission Menu"
      description="Configure the Admission mega-menu items."
    />
  );
}
