import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminAddCourses() {
  return (
    <AdminPagePlaceholder
      title="Admin — Add Course"
      description="Create a new course entry with details, fees, and schedule."
    />
  );
}
