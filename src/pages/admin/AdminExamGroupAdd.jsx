import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminExamGroupAdd() {
  return (
    <AdminPagePlaceholder
      title="Admin — Add Exam Group"
      description="Create a new exam group (e.g. semester, subject cluster)."
    />
  );
}
