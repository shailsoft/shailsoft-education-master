import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminEventAdd() {
  return (
    <AdminPagePlaceholder
      title="Admin — Add Event"
      description="Create a new event with date, speakers, and venue."
    />
  );
}
