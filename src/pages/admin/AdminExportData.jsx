import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminExportData() {
  return (
    <AdminPagePlaceholder
      title="Admin — Export Data"
      description="Export students, courses, and enquiry data to CSV/XLSX."
    />
  );
}
