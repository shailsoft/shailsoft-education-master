import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminSliderEdit() {
  return (
    <AdminPagePlaceholder
      title="Admin — Edit Slide"
      description="Update a single slider entry."
    />
  );
}
