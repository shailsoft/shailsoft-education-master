import AdminPagePlaceholder from '../../components/admin/AdminPagePlaceholder.jsx';

export default function AdminPageAdd() {
  return (
    <AdminPagePlaceholder
      title="Admin — Add Page"
      description="Create a new content page for the website."
    />
  );
}
