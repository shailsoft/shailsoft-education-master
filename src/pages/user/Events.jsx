import PagePlaceholder from '../../components/common/PagePlaceholder.jsx';

export default function Events() {
  return (
    <PagePlaceholder
      title="Events"
      description="Campus events, open days, and community activities throughout the year."
      crumbs={[{ label: 'Events' }]}
    />
  );
}
